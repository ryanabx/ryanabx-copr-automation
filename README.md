# COPR Automation Script for my COSMIC maintenance

If you happened to find this script, this script (and associated GitHub action) keeps the nightly copr going by checking whether a new commit has been release in the corresponding component's upstream repo.

It also facilitates test builds of the tagged releases for validation before I release them upstream.